{
  "name": "cms-access-auth",
  "private": true,
  "version": "1.0.0",
  "description": "Reusable Cloudflare Access -> GitHub token bridge for Decap CMS. Editors log in with email only; the GitHub token stays server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
