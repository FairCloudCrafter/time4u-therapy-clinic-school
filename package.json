{
  "name": "time4u-therapy-clinic-school",
  "private": true,
  "workspaces": [
    "site"
  ],
  "scripts": {
    "install-site": "npm install --workspace site",
    "build": "npm run build --workspace site",
    "dev": "npm run dev --workspace site",
    "start": "npm run start --workspace site"
  }
}
